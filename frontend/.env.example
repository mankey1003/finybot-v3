# Copy to .env.local for local development (never commit .env.local)
# All VITE_ vars are bundled into the client — do NOT put secrets here.

# Firebase project config (from Firebase Console → Project Settings → Your apps)
VITE_FIREBASE_API_KEY=
VITE_FIREBASE_AUTH_DOMAIN=your-project.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=your-project-id
VITE_FIREBASE_STORAGE_BUCKET=your-project.appspot.com
VITE_FIREBASE_MESSAGING_SENDER_ID=
VITE_FIREBASE_APP_ID=

# Backend base URL — leave empty to use Vite proxy (localhost dev)
# Production: https://finybot-backend-xxxx-uc.a.run.app  (or your Firebase Hosting domain)
VITE_API_BASE_URL=
